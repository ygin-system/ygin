Изменения в проектных файлах:

20120913
Переименованы виджеты:
DaMenuWidget -> MenuWidget (используется в шаблоне тем)
DaBlockWidget -> BlockWidget (используется в шаблоне тем)
DaAlertWidget -> AlertWidget (возможны использования в темах или прикладных представлениях)
DaLikeAndShare -> LikeAndShareWidget (возможны использования в темах или прикладных представлениях)
DaLinkPager -> LinkPagerWidget (возможны использования в темах или прикладных представлениях)
DaBreadcrumbs -> BreadcrumbsWidget (используется в шаблоне тем)

Поменялся путь у плагина Витрины. Поэтому необходимо поменять путь в конфиг файле:
      'ygin.vitrine' => array('class' => 'ygin.widgets.vitrine.VitrinePlugin'),


20120918
Модуль интернет-магазин:

Виджеты категорий и брендов разнесены по папочкам, а также у них разделены представления и ресурсы
shop.widgets.BrandWidget -> shop.widgets.brand.BrandWidget
shop.widgets.CategoryWidget -> shop.widgets.category.CategoryWidget

20121022
Необходимо перевести проектную реализацию callBeforee и callAfter-методов на yii в следующих проектных файлах:
project/class/engine/UserProject.php
project/class/engine/FilesProject.php
project/class/engine/SMenuProject.php
project/class/engine/DomainProject.php

Скорей всего, там ничего не будет. Но если есть, то надо перенести в соответствующие модели на yii.
Старую реализацию удалять не надо.

+ обязательно добавить для всех проектных таблиц автоинкремент

20121031
Обработчики объектов переходят на yii-реализацию. В связи с этим, все проектные наработки на старом engine необходимо перенести на yii.
1. Необходимо посмотреть обработчики системных объектов, которые могли быть изменены в проекте. И если там реализованы методы, то перенести их в соответствующие классы на yii:
project/objectHandlers/DomainProcess.php 
project/objectHandlers/FileProcess.php
project/objectHandlers/user_process.php
project/modules/vote/vote_process.php
project/objectHandlers/rss_process.php
project/objectHandlers/SystemModuleProcess.php

20121209
1. Удален старый обработчик объекта Пользователи + его старый класс для экземпляра. Если в проектной части были данные классы, то их логику необходимо перенести на yii.
Смотреть тут:
project\class\engine\UserProject.php

2. Больше не используются старые понятия Пользователь, Группа пользователей и Сессия. Если на сайте использовались свои группы пользователей, то их надо оформить через роли.
В ближайших апдейтах таблица с группами будет удалена.

20121227
Теперь модуль Messender по-умолчанию отключен.
Если он использовался на сайте, то нужно его подключить в project.php.

20130207
Папка с фреймворком из корня перенесена в папку ygin.
Поэтому в index.php в корне необходимо заменить
require_once(dirname(__FILE__).'/yii/framework/YiiBase.php'); на
require_once(dirname(__FILE__).'/ygin/yii/YiiBase.php');
и удалить папку с фреймворком из корня.

20130219
Удален метод HU::translit. Необходимо использовать HText::translit

20130222
Папки engine, engine-module, project переехали в ygin.
В связи с этим необходимо все ссылки на engine файлы в проекте переделать на новые.
Как правило это касается только лейаута темы, т.к. там использовались скрипты и стили из папки engine.
Необходимо поглядеть как сейчас сделано в проекте engine_macro themes/business/views/layouts/main.php и переделать в своих файлах.

20130223
Чуть переделана схема работы виз элементов. Из общего шаблона (layout) виз элемента вынесена конструкция выводящая ошибки. Теперь это надо делать непосредственно в представлении каждого виз элемента.
В связи с этим, если в проектах есть виз элементы, то надо их доработать выводом ошибок (по необходимости)

20130317
Теперь в title страниц добавляется не доменное имя, а описание домена. Поэтому следует проверить описание доменного имя на адекватность.

20130404
У контроллера появилось булевское свойство useBreadcrumbs. Его необходимо внедрить в шаблон темы если на сайте используется цепочка навигации:
if ($this->useBreadcrumbs && isset($this->breadcrumbs)): // Цепочка навигации
...

20130407
Теперь в галерее у фоток выводятся их названия. Если этого не нужно, то следует переопеределить представление виджета и удалить оттуда вывод фото.
И наоборот, если виджет фоток переопеределен и нужно выводить название, то следует поглядеть изменения в виджете и внедрить их к себе.


20130408
Для внешней части сайта теперь используется прикладной бутстрап, который находится в папке /protected/assets/ и может быть легко заменён и отредактирован для нужд сайта.
Вместо подключения файла в шаблоне
  $this->registerJsFile('bootstrap.js', 'ygin.assets.bootstrap.js');
  $this->registerCssFile('bootstrap.min.css', 'webroot.themes.business.bootstrap.css');
Стоит использовать подключение из пакета:
  Yii::app()->clientScript->registerCoreScript('bootstrap'); 

Данный файл - webroot.themes.business.bootstrap.css - стоит на всякий случай перенести в /protected/assets/bootstrap/css, т.к. он может содержать проектные кастомизации


20130420
В связи с переходом на новый алгоритм хеширования паролей была изменена модель User:
удалены методы User::hashPassword, User::getIsPasswordChanged, поэтому если вы использовали их,
то эти куски кода надо переписать.

Перевод сайтов на Yii (старых) на новый алгоритм хеширования:
1) Скопировать с engine_macro папку ygin/modules/user/behaviors/password
2) Добавить в таблицу da_users следующие поля:
ALTER TABLE `da_users` ADD `requires_new_password` TINYINT(1) COMMENT  'Необходимо сменить пароль';
ALTER TABLE  `da_users` ADD  `salt` VARCHAR( 255 ) NULL COMMENT  'Соль для пароля',
    ADD  `password_strategy` VARCHAR( 255 ) NULL COMMENT  'Стратегия для формирования пароля';
3) прогнать запрос UPDATE `da_users` SET `password_strategy` = 'legacy';
4) подключить в модели User поведения

  public function behaviors() {
    return array(
      "APasswordBehavior" => array(
        "class" => "APasswordBehavior",
        "defaultStrategyName" => "bcrypt",
        "passwordAttribute" => "user_password",
        "strategies" => array(
          "bcrypt" => array(
            "class" => "DaBcryptPasswordStrategy",
            "workFactor" => 14
          ),
          "legacy" => array(
            "class" => "ALegacyMd5PasswordStrategy",
          )
        ),
      ),
    );
  }
5) убрать использование методов  User::hashPassword, User::getIsPasswordChanged,
 и формирования хеша пароля напрямую, т.е. $user->user_password = md5($password);
 Нужно использовать только $user->save(), поведения сделают за вас всю работу.

20130515
Необходимо удалить папку log из корня сайта. Чтобы логи не потерялись, можно перенести папку log из корня сайта в папку protected/runtime/

20130522
Переменные приложения backend и fronted переименовались соответственно в isBackend и isFrontend
Модуль админки yiigin переименовался в backend. Если в проекте использовались папки yiigin для хранения виджетов или обработчиков, то необходимо поменять имя на backend

20130523
Корневая папка ngin переименовалась в ygin
Автоматически все упоминания "ngin." в исходном коде поменяются на "ygin.". Сайт и систему управления необходимо тщательно протестировать на работоспособность.
Бывает такое, что на часть файлов может не быть прав, тогда скрипту не получится автоматически, например, переименовать папку ngin в ygin. Или может быть такое, что после отработки миграции останутся две папки (ngin и ygin) - в таком случае необходимо перенести всё содержимое папки ngin в ygin вручную.

20130611
У модели Menu переименованы два поля:
zagolovok => caption
idParent => id_parent

20130619
У модулей faq, review, feedback изменился способ формирования сообщения, уходящего на email, после сохранения модели. 
Теперь вместо вызова события используется renderPartial, соответственно, если эти события использовались, то необходимо 
переопределить представления сообщений через механизм тем. 

20130619
ИД объектов, параметров и тд теперь вместо точки должны содержать дефис в качестве разделителя.
ИД не может содержать строку без дефиса.

20130620
Все настройки по отправке почты вынесены в конфиг local.php
Внимание! Если их там не прописать, то больше не будут приходить ошибки на почту.

20130724
В списке новостей теперь для вывода краткого содержания применяется функция nl2br
Если такого не надо (например если краткое содержание переделано с textarea на редактор), то стоит переопределить представление (если этого ещё не сделано)

20130919
Переверстан модуль комментариев. Если он использовался на сайте, то необходимо проверить отображение и его работу.
В частности, у виджет ygin.modules.comments.widgets.ECommentsListWidget больше не является предком виджета CJuiWidget, поэтому у него нет его свойств. В частности, могли использоваться такие свойства:
themeUrl
cssFile
В случае их применения на сайте, их надо удалить из вызова.
Также у поведения комментариев удален метод relationCountComment. Вместо этого в представлениях виджета доступна переменная $count.
Ещё было удалено свойтсво виджета showPopupForm. Теперь у нас нет возможности использовать попап-окна.
И был удален файл ajax-loader.gif

20131120
Если контроллер не связан с разделом меню, то теперь используется набор виджетов по умолчанию, а не набор виджетов главной страницы.

20140110
На бэкэнд-приложении обновлена версия твиттер-бутрстапа со 2 до 3 версии.
Поэтому, если в проекте используется твиттер-бутстрап 2 версии, то необходимо убедиться, что не используются ресурсы с сисемной папки.
Как правило, необходимо поправить в layout путь с ygin.assets.bootstrap.img на application.assets.bootstrap.img
И положить в папку application.assets.bootstrap.img два новых файла: glyphicons-halflings-green.png и glyphicons-halflings-red.png (взять можно, например, с инсталлятора системы)

